## 给谁看

- 如果你是一个熟悉react的开发者，想对项目进行改动；
- 想直接修复线上代码的bug；
- 觉得用docker启动一个前后端完整项目太麻烦

## 如何单独启动

- git clone 项目
- 进入 apps/web 文件夹
- 运行 pnpm i 安装依赖
- 在 vite.config 中修改代理配置

此项目为了防止csrf攻击，在接口增加了白名单校验，但本地调试者肯定是知道自己在做什么

找到这个路径 `apps\web\vite.config.ts`，按照下方修改
rss 和 healthz 想不想改看情况

```js
const coreProxy = {
   '/api': {
      target: 'https://rousi.pro',
      changeOrigin: true,
      configure: (proxy, options) => {
        proxy.on('proxyReq', (proxyReq, req, res) => {
 
          // 假设你在 allowedOrigins 中配置了 "https://rousi.pro" 或者是后端本身的地址
          const trustOrigin = 'https://rousi.pro' 
          
          // 伪造 Origin 和 Referer 头部，使其命中 Go 中间件的白名单
          proxyReq.setHeader('Origin', trustOrigin)
          proxyReq.setHeader('Referer', trustOrigin + '/')
        })
      },
    },
  "/rss": coreTarget,
  "/healthz": coreTarget,
}
```

- 运行 pnpm run dev 启动项目